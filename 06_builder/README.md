# 生成器模式

生成器模式（Builder）将一个复杂对象的构建过程与其表示分离，使得同样的构建过程可以创建不同的表示。

生成器模式包含以下角色：

* `Builder` 接口定义构建各部件的方法。
* 具体生成器（如 `Builder1`、`Builder2`）实现 `Builder` 接口，分别构建不同表示的产品。
* `Director` 负责使用 `Builder` 接口按照固定步骤（`Part1`、`Part2`、`Part3`）完成构建过程。

客户端只需选择具体的生成器传入 `Director`，即可得到不同表示的构建结果，而无需关心构建细节。
